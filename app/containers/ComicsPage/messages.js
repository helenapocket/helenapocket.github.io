/*
 * ComicsPage Messages
 *
 * This contains all the text for the ComicsPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ComicsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "comics page",
  },
});
