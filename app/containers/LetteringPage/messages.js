/*
 * LetteringPage Messages
 *
 * This contains all the text for the LetteringPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LetteringPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "lettering page",
  },
});
