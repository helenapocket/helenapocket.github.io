/*
 * EnvironmentsPage Messages
 *
 * This contains all the text for the EnvironmentsPage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EnvironmentsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "environments page",
  },
});
